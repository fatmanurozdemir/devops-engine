apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: login-gateway
spec:
  selector:
    istio: ingressgateway
  servers:
  - port:
      number: 5000
      name: http
      protocol: HTTP
    hosts:
    - "*"
